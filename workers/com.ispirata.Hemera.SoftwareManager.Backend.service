[D-BUS Service]
Name=com.ispirata.Hemera.SoftwareManager.Backend
SystemdService=gravity-software-manager-backend.service

# This service should not be bus activated if systemd isn't running,
# so that activation won't conflict with the init script startup.
Exec=/bin/false
